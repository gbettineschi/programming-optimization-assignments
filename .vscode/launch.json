{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File from its directory",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${fileDirname}",
            "stopOnEntry": true,
            "justMyCode": false
        }
    ]
}